<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:padding="5dp"
    android:theme="@style/AppTheme"
    android:layout_marginTop="5dp"
    android:layout_marginEnd="5dp"
    >


    <TextView
        android:id="@+id/spinner_colores_item_TextViewDisponibilidad"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/spinner_colores_item_TextViewColor"
        android:layout_alignEnd="@+id/spinner_colores_item_TextViewColor"
        android:layout_alignParentBottom="true"
        android:text="Agotado!"
        android:textColor="@color/colorBlack"
        android:textSize="16sp"
        android:gravity="center"
        />

    <TextView
        android:id="@+id/spinner_colores_item_TextViewColor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginEnd="20dp"
        android:layout_toStartOf="@+id/cardView1"
        android:text="Color"
        android:textColor="@color/colorBlack"
        android:textSize="20sp" />

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="20dp"
        android:elevation="12dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true">



        <ImageView
            android:id="@+id/spinner_colores_item_imageView"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:scaleType="centerCrop"
            android:contentDescription="Contendra la imagen de muestra del color o solamente un color relleno impuesto por el backgroun color"
            />

    </androidx.cardview.widget.CardView>





    
</RelativeLayout>